# Test Environment Variables
NODE_ENV=test

# Database (use different database for tests)
DATABASE_URL=postgresql://username:password@localhost:5432/walletrix_test?schema=public

# API Configuration
API_PORT=3002
FRONTEND_URL=http://localhost:3000

# Security
JWT_SECRET=test-jwt-secret-key-for-testing-only-not-for-production
JWT_REFRESH_SECRET=test-refresh-secret-key-for-testing-only-not-for-production
ENCRYPTION_KEY=test-encryption-key-32-chars-long

# External APIs (use test/mock endpoints)
ETHEREUM_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/test
POLYGON_RPC_URL=https://polygon-mumbai.g.alchemy.com/v2/test
BITCOIN_RPC_URL=https://blockstream.info/testnet/api
COINGECKO_API_KEY=test-api-key

# Email (use test transport)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=test@example.com
EMAIL_PASS=test-password
EMAIL_FROM=noreply@walletrix.com

# SMS (use test provider)
TWILIO_ACCOUNT_SID=test-account-sid
TWILIO_AUTH_TOKEN=test-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Rate Limiting (more lenient for tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# Logging
LOG_LEVEL=error
LOG_FILE=false